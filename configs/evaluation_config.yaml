benchmark:
  path: data/benchmark/gold_benchmark.jsonl
  min_questions: 150

retrieval_eval:
  k_values: [1, 3, 5, 10]
  systems:
    - baseline_dense
    - bm25_only
    - hybrid
    - hybrid_reranked

qa_eval:
  metrics:
    - f1
    - exact_match

output:
  results_dir: outputs/evaluation
  comparison_path: outputs/evaluation/comparison_report.json
